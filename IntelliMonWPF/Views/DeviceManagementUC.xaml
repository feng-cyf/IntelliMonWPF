<UserControl x:Class="IntelliMonWPF.Views.DeviceManagementUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IntelliMonWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <UniformGrid Columns="5">
            <!-- 添加设备：主蓝色（新增操作，视觉突出） -->
            <Button Content="添加设备" Command="{Binding AddDeviceCommand}" Margin="10"  
        Background="#165DFF" Foreground="White" 
        BorderBrush="#165DFF" BorderThickness="1"/>

            <!-- 编辑设备：天蓝色（修改操作，温和区分） -->
            <Button Content="编辑设备" Command="{Binding EditDeviceCmd}" Margin="10"  
        Background="#36CFC9" Foreground="White" 
        BorderBrush="#36CFC9" BorderThickness="1"/>

            <!-- 启动设备：绿色（正向操作，代表运行） -->
            <Button Content="启动设备" Command="{Binding RefreshCommand}" Margin="10"  
        Background="#52C41A" Foreground="White" 
        BorderBrush="#52C41A" BorderThickness="1"/>

            <!-- 停止设备：橙色（警示操作，代表暂停） -->
            <Button Content="停止设备" Command="{Binding ExportCommand}" Margin="10"  
        Background="#FAAD14" Foreground="White" 
        BorderBrush="#FAAD14" BorderThickness="1"/>

            <!-- 删除设备：红色（危险操作，强提醒） -->
            <Button Content="删除设备" Command="{Binding ImportCommand}" Margin="10"  
        Background="#FF4D4F" Foreground="White" 
        BorderBrush="#FF4D4F" BorderThickness="1"/>
        </UniformGrid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DataGrid ItemsSource="{Binding Devices}" 
                      SelectedItem="{Binding SelectedDevice}"
                      AutoGenerateColumns="False"
                      CanUserResizeRows="True"
                      IsReadOnly="True"
                      Margin="0,0,15,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="设备名称" Binding="{Binding Name}" Width="150"/>
                    <DataGridTextColumn 
                                    Header="类型" 
                                    Binding="{Binding Type}" 
                                    Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="ToolTip" Value="{Binding Type}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="IP/串口" Binding="{Binding ConnectionString}" Width="120"/>
                    <DataGridTextColumn Header="端口" Binding="{Binding Port}" Width="60"/>
                    <DataGridTextColumn Header="从站ID" Binding="{Binding SlaveId}" Width="100"/>
                    <DataGridTextColumn Header="轮询间隔(ms)" Binding="{Binding PeriodTime}" Width="150"/>
                    <DataGridTextColumn Header="功能码" Binding="{Binding Function.Key}" Width="80"/>
                    <DataGridTemplateColumn Header="状态" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Ellipse Width="10" Height="10"/>
                                    <TextBlock Text="{Binding Status}" Margin="0,0,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="1" Background="White" CornerRadius="5" BorderBrush="#DDDDDD" BorderThickness="1" Margin="0 0">
                <StackPanel Margin="15">
                    <TextBlock Text="设备筛选" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                    <Expander Header="筛选条件" IsExpanded="True" Margin="0,0,0,10">
                        <Grid Margin="10 5 0 5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="设备名称:" Grid.Row="0" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding FilterName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="5,0,0,0"/>
                            <TextBlock Text="设备IP:" Grid.Row="1" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding FilterName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="5,0,0,0"/>
                            <TextBlock Text="设备运行状态:" Grid.Row="2" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding FilterName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Margin="5,0,0,0"/>
                            <Button Content="筛选查找" Command="{Binding ApplyFilterCommand}" Grid.Row="3" Grid.ColumnSpan="2" Margin="0,10,0,0" 
                                    Background="#165DFF" Foreground="White" 
                                    BorderBrush="#165DFF" BorderThickness="1"/> 
                        </Grid>
                    </Expander>
                    <TextBlock Text="设备详情" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

                    <!-- 基本信息 -->
                    <Expander Header="基本信息" IsExpanded="True" Margin="0,0,0,10">
                        <Grid Margin="10,5,0,5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="设备名称:" Grid.Row="0"/>
                            <TextBlock Text="{Binding SelectedDevice.Name}" Grid.Row="0" Grid.Column="1"/>

                            <TextBlock Text="设备类型:" Grid.Row="1" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding SelectedDevice.Type}" Grid.Row="1" Grid.Column="1" Margin="0,5,0,0"/>
                        </Grid>
                    </Expander>

                    <!-- 通信参数 -->
                    <Expander Header="通信参数" IsExpanded="True" Margin="0,0,0,10">
                        <Grid Margin="10,5,0,5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="连接地址:" Grid.Row="0"/>
                            <TextBlock Text="{Binding SelectedDevice.ConnectionString}" Grid.Row="0" Grid.Column="1"/>

                            <TextBlock Text="端口号:" Grid.Row="1" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding SelectedDevice.Port}" Grid.Row="1" Grid.Column="1" Margin="0,5,0,0"/>

                            <TextBlock Text="从站ID:" Grid.Row="2" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding SelectedDevice.SlaveId}" Grid.Row="2" Grid.Column="1" Margin="0,5,0,0"/>

                            <TextBlock Text="读取超时(ms):" Grid.Row="3" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding SelectedDevice.ReadModel.ReadTimeout}" Grid.Row="3" Grid.Column="1" Margin="0,5,0,0"/>
                            <TextBlock Text="发送超时(ms):" Grid.Row="4" Margin="0,5,0,0"/>
                            <TextBlock Text="" Grid.Row="4" Grid.Column="1" Margin="0,5,0,0"/>
                        </Grid>
                    </Expander>

                    <!-- 运行参数 -->
                    <Expander Header="运行参数" IsExpanded="True" Margin="0,0,0,10">
                        <Grid Margin="10,5,0,5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="轮询间隔:" Grid.Row="0"/>
                            <TextBlock Text="{Binding SelectedDevice.PeriodTime}" Grid.Row="0" Grid.Column="1"/>

                            <TextBlock Text="当前状态:" Grid.Row="1" Margin="0,5,0,0"/>
                            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,5,0,0">
                                <Ellipse Width="10" Height="10"/>
                                <TextBlock Text="{Binding SelectedDevice.Status}" Margin="5,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Expander>
                    <Expander Header="其他信息" IsExpanded="True" Margin="0,0,0,10">
                        <Grid Margin="10,5,0,5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Text="最后通信时间:" Grid.Row="0" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding SelectedDevice.Notes}" Grid.Row="1" Grid.Column="1" Margin="0,5,0,0" TextWrapping="Wrap"/>
                            <TextBlock Text="错误计数:" Grid.Row="1" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding SelectedDevice.Notes}" Grid.Row="1" Grid.Column="1" Margin="0,5,0,0" TextWrapping="Wrap"/>
                        </Grid>
                    </Expander>
                    <Button Content="导入csv" Margin="0 10"></Button>
                    <Button Content="报文查看" Margin="0 10" Command="{Binding ShowPackUCCmdCommand}"></Button>
                </StackPanel>
            </Border>

        </Grid>

    </Grid>
</UserControl>
