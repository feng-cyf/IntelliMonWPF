<UserControl x:Class="IntelliMonWPF.Views.AddDeviceModbusUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IntelliMonWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <UserControl.Resources>
        <!-- 定义统一样式 -->
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="10,5,10,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Text="设备添加" FontSize="20" FontWeight="Bold" 
                   VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="0"/>

        <!-- 输入区域 -->
        <StackPanel Grid.Row="1" Margin="0,10">
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="设备名称:" Grid.Column="0"/>
                    <ComboBox Width="200" HorizontalAlignment="Right" Grid.Column="0" ItemsSource="{Binding DeviceNameList}" SelectedItem="{Binding DeviceName}"/>
                </StackPanel>
            </Grid>
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="从站地址:" Grid.Column="0"/>
                <TextBox Grid.Column="1" ToolTip="请输入从站地址（1-247）" Text="{Binding SlaveId}"/>
            </Grid>
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="起始位置:" Grid.Column="0"/>
                <TextBox Grid.Column="1" ToolTip="请输入起始寄存器地址" Text="{Binding StartAdress}"/>
            </Grid>
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="读取数量:" Grid.Column="0"/>
                <TextBox Grid.Column="1" ToolTip="请输入读取寄存器数量" Text="{Binding ReadCount}"/>
            </Grid>
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="循环时间(ms):" Grid.Column="0"/>
                <TextBox Grid.Column="1" ToolTip="请输入循环时间（毫秒）" Text="{Binding Intervate}"/>
            </Grid>
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="功能码:" Grid.Column="0"/>
                <ComboBox Grid.Column="1" ToolTip="请选择Modbus功能码" ItemsSource="{Binding FunctionList}" SelectedItem="{Binding SelectFunction}" DisplayMemberPath="Value">
                    
                </ComboBox>
            </Grid>
        </StackPanel>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="取消" IsCancel="True" Command="{Binding CanelCmd}"/>
            <Button Content="确定" IsDefault="True" Command="{Binding AddCmd}"/>
        </StackPanel>
    </Grid>
</UserControl>